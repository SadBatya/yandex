const sliderBtnPrev = document.querySelector(".btn-left");
const sliderBtnNext = document.querySelector(".btn-right");

const container = document.querySelector(".slider");
const sliderContainer = document.querySelector(".slider__cards");
const cards = sliderContainer.querySelector('.slider__card');

export const  sliderMove = () => {
  console.log(container);
  console.log(sliderContainer);
  console.log(cards);
};

